package com.codingshuttle.projects.lovable_clone.enums;

/**
 * Enum representing the role of a message sender in a chat session.
 * Used to differentiate between user input, AI responses, system messages, and tool outputs.
 */
public enum MessageRole {
    USER, ASSISTANT, SYSTEM, TOOL
}
