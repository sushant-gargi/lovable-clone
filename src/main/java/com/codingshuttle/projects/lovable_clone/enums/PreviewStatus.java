package com.codingshuttle.projects.lovable_clone.enums;

/**
 * Enum representing the current status of a project preview environment.
 * Used to track the lifecycle of a preview deployment.
 */
public enum PreviewStatus {
    CREATING, RUNNING, FAILED, TERMINATED
}
